# Expected Outputs Directory

This directory contains expected test results and outputs for validation:

- **Graph Outputs**: Expected JSON representations of code graphs
- **RepoMap Outputs**: Expected rendering outputs (full, compressed, hierarchical)
- **Configuration Outputs**: Expected configuration parsing results
- **Error Outputs**: Expected error messages and stack traces

These files are used by tests to validate that Batho produces the correct outputs for given inputs.
